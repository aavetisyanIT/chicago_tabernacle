import React from 'react';
import CustomCard from './custom-card.component';
import {TouchableRipple} from 'react-native-paper';
import {Text, View} from 'react-native';

const SermonCard = () => {
  return (
    <View>
      {/* <TouchableRipple> */}
      <Text>Sermon Card</Text>
      {/* </TouchableRipple> */}
    </View>
  );
};

export default SermonCard;
